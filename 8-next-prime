public Integer nextPrime(Integer num) {
    //code here
    if(num < 2) {
        return 2;
    }
    num++;
    for(Integer i=2; i<= num/2; i++) {
        if(Math.mod(num,i) == 0) {
            i = 2;
            num++;
        }
    }

    return num;
}