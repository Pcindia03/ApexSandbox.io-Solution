public String formatName(String firstName, String lastName) {
    //code here
    if (!String.isEmpty(firstName) && !String.isEmpty(lastName)) {
        return lastName + ', ' + firstName;
    }
    else if (!String.isEmpty(firstName)) {
        return firstName;
    }
    else if (!String.isEmpty(lastName)) {
        return lastName;
    }
    return '';
}